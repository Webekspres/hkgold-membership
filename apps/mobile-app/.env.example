# Copy keys ke Doppler project `hkgoldvip` / config `dev_mobile`.
# WAJIB ada di Doppler — tanpa key ini login error "URL kosong".
# Nilai = http://<IP-LAN-mesin-api>:3000  (bukan localhost — HP harus reach).
# Contoh laptop ini: http://192.168.0.109:3000
# Contoh node eksternal tim: http://192.168.0.193:3000
# Jangan commit secret. Jalankan app: npm start  (via `doppler run`)

EXPO_PUBLIC_API_URL=http://192.168.0.109:3000

# Nomor WhatsApp admin (lupa password — WA belum diatur). Digit saja, tanpa +.
EXPO_PUBLIC_ADMIN_WHATSAPP=6282258119788

# Firebase (project sama dengan Filament). File lokal, jangan commit — lihat .gitignore:
#   apps/mobile-app/google-services.json
#   apps/mobile-app/GoogleService-Info.plist
