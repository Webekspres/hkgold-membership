export function formatRewardPoints(points: number) {
  return `${points.toLocaleString('id-ID')} poin`;
}

export function formatStockRemaining(count: number) {
  return `${count.toLocaleString('id-ID')} tersisa`;
}
